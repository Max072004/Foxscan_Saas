import type { AppData, User } from "./domain"; import { id } from "./store"; import { integrations } from "./integrations";
export async function notify(data:AppData, recipient:User, event:string,title:string,body:string){const channels:("IN_APP"|"WHATSAPP"|"EMAIL"|"PUSH")[]=["IN_APP"];data.notifications.push({id:id(),tenantId:recipient.tenantId,recipientId:recipient.id,event,title,body,channels,createdAt:new Date().toISOString()});const jobs=[recipient.phone?integrations.whatsapp.send({to:recipient.phone,subject:title,body}).then(()=>channels.push("WHATSAPP")).catch(()=>undefined):undefined,recipient.email?integrations.email.send({to:recipient.email,subject:title,body}).then(()=>channels.push("EMAIL")).catch(()=>undefined):undefined];await Promise.all(jobs);return channels;}
