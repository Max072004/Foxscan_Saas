import * as SecureStore from "expo-secure-store"; import { create } from "zustand"; import type {Role} from "@/lib/types";
type AuthState={token:string|null;userId:string|null;role:Role|null;name:string|null;hydrate:()=>Promise<void>;signIn:(token:string,user:{id:string;role:Role;name:string})=>Promise<void>;signOut:()=>Promise<void>;updateName:(name:string)=>Promise<void>};
export const useAuthStore=create<AuthState>((set,get)=>({token:null,userId:null,role:null,name:null,hydrate:async()=>{const raw=await SecureStore.getItemAsync("foxscan.session");if(raw)set(JSON.parse(raw));},signIn:async(token,user)=>{const next={token,userId:user.id,role:user.role,name:user.name};await SecureStore.setItemAsync("foxscan.session",JSON.stringify(next));set(next);},signOut:async()=>{await SecureStore.deleteItemAsync("foxscan.session");set({token:null,userId:null,role:null,name:null});},updateName:async(name)=>{const {token,userId,role}=get();set({name});await SecureStore.setItemAsync("foxscan.session",JSON.stringify({token,userId,role,name}));}}));
