import {useQuery}from"@tanstack/react-query";import{Text}from"react-native";import{Screen,Title,Card,Button}from"@/components/ui";import{api}from"@/lib/api";
export default function Approvals(){const{data,refetch}=useQuery({queryKey:["data"],queryFn:()=>api<any>("/api/data")});const act=async(stageId:string,action:"APPROVE"|"RETURN")=>{await api("/api/stages",{method:"PATCH",body:JSON.stringify({stageId,action,role:"CONSULTANT",actorId:"mobile"})});refetch()};return <Screen><Title>Approvals</Title>{data?.stages?.filter((s:any)=>s.state!=="PAID").map((stage:any)=><Card key={stage.id}><Text className="font-bold">Awaiting {stage.state}</Text><Text className="mb-3 text-slate-500">Due {new Date(stage.dueAt).toLocaleString()}</Text><Button label="Approve" onPress={()=>act(stage.id,"APPROVE")}/></Card>)}</Screen>}
