import{Text}from"react-native";import{useQuery}from"@tanstack/react-query";import{Screen,Title,Card}from"@/components/ui";import{api}from"@/lib/api";
export default function Notifications(){const{data}=useQuery({queryKey:["data"],queryFn:()=>api<any>("/api/data")});return <Screen><Title>Notifications</Title>{data?.notifications?.length?data.notifications.map((n:any)=><Card key={n.id}><Text className="font-bold">{n.title}</Text><Text>{n.body}</Text></Card>):<Card><Text className="text-slate-500">No notifications yet. Push registration is enabled once EAS project credentials are configured.</Text></Card>}</Screen>}
