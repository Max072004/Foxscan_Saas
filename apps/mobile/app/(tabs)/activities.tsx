import {useQuery}from"@tanstack/react-query";import{Text}from"react-native";import{Screen,Title,Card}from"@/components/ui";import{api}from"@/lib/api";import type{Activity}from"@/lib/types";
export default function Activities(){const{data}=useQuery({queryKey:["data"],queryFn:()=>api<{activities:Activity[]}>("/api/data")});return <Screen><Title>Activities</Title>{data?.activities.map(a=><Card key={a.id}><Text className="font-bold">{a.name}</Text><Text className="text-slate-500">{a.plannedStart} — {a.plannedEnd}</Text><Text className="text-fox">{a.status} · {a.progress}%</Text></Card>)}</Screen>}
