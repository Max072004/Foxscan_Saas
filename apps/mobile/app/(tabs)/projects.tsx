import {useQuery} from "@tanstack/react-query"; import {Text,Pressable} from "react-native"; import {useRouter} from "expo-router"; import {Screen,Title,Card} from "@/components/ui"; import {api} from "@/lib/api"; import type{Project}from"@/lib/types";
export default function Projects(){const{data=[],error}=useQuery({queryKey:["projects"],queryFn:()=>api<Project[]>("/api/projects")});const router=useRouter();return <Screen><Title>Projects</Title>{error?<Text className="text-red-600">{error.message}</Text>:null}{data.map(project=><Pressable key={project.id} onPress={()=>router.push(`/project/${project.id}`)}><Card><Text className="font-bold text-lg">{project.name}</Text><Text className="text-slate-500">{project.address}</Text><Text className="text-fox mt-2">{project.status}</Text></Card></Pressable>)}</Screen>}
