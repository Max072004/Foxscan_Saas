import {useLocalSearchParams}from"expo-router";import{useQuery}from"@tanstack/react-query";import{Text}from"react-native";import{Screen,Title,Card}from"@/components/ui";import{api}from"@/lib/api";import type{Project}from"@/lib/types";
export default function ProjectDetails(){const{id}=useLocalSearchParams<{id:string}>();const{data}=useQuery({queryKey:["projects"],queryFn:()=>api<Project[]>("/api/projects")});const project=data?.find(p=>p.id===id);return <Screen><Title>{project?.name||"Project"}</Title><Card><Text>{project?.address}</Text><Text className="mt-2">Contract value: ₹{project?.contractValue?.toLocaleString("en-IN")}</Text><Text>Schedule: {project?.startDate} — {project?.endDate}</Text></Card></Screen>}
