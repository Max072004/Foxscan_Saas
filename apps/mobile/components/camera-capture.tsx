import {useRef,useState}from"react";import{Text,View,Image}from"react-native";import{CameraView,useCameraPermissions}from"expo-camera";import*as ImageManipulator from"expo-image-manipulator";import*as Location from"expo-location";import{Button}from"@/components/ui";
export function CameraCapture({onCapture}:{onCapture:(capture:{uri:string;latitude?:number;longitude?:number})=>void}){const camera=useRef<CameraView>(null);const[permission,requestPermission]=useCameraPermissions();const[preview,setPreview]=useState<string>();if(!permission?.granted)return <View><Text className="mb-2">Camera access is required for photo evidence.</Text><Button label="Allow camera" onPress={()=>requestPermission()}/></View>;const capture=async()=>{const photo=await camera.current?.takePictureAsync({quality:0.7});if(!photo)return;const compressed=await ImageManipulator.manipulateAsync(photo.uri,[],{compress:0.65,format:ImageManipulator.SaveFormat.JPEG});const locationPermission=await Location.requestForegroundPermissionsAsync();const location=locationPermission.status==="granted"?await Location.getCurrentPositionAsync({accuracy:Location.Accuracy.Balanced}):undefined;setPreview(compressed.uri);onCapture({uri:compressed.uri,latitude:location?.coords.latitude,longitude:location?.coords.longitude});};return <View>{preview?<Image source={{uri:preview}} className="h-48 rounded-xl mb-3"/>:<CameraView ref={camera} style={{height:260,borderRadius:12,overflow:"hidden"}} facing="back"/>}<Button label="Capture & compress evidence" onPress={capture}/></View>}
