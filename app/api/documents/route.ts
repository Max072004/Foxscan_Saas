import { NextRequest,NextResponse } from "next/server"; import { getData,id,saveData,audit } from "@/lib/store"; import { currentUser } from "@/lib/auth";
export async function GET(req:NextRequest){try{const data=await getData(),user=currentUser(data,req.headers.get("authorization"));const q=req.nextUrl.searchParams.get("q")?.toLowerCase()||"";const projectId=req.nextUrl.searchParams.get("projectId");return NextResponse.json(data.documents.filter(d=>d.visibility.includes(user.role)&&(!projectId||d.projectId===projectId)&&(!q||`${d.name} ${d.type} ${d.tags.join(" ")}`.toLowerCase().includes(q))));}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unauthorized"},{status:401});}}
export async function POST(req:NextRequest){try{const data=await getData(),user=currentUser(data,req.headers.get("authorization"));const body=await req.json();const prior=data.documents.filter(d=>d.projectId===body.projectId&&d.name===body.name);const document={id:id(),uploadedBy:user.id,version:prior.length?Math.max(...prior.map(d=>d.version))+1:1,createdAt:new Date().toISOString(),...body};data.documents.push(document);await audit(data,{tenantId:user.tenantId,actorId:user.id,action:"UPLOAD",entity:"document",entityId:document.id,after:document});await saveData(data);return NextResponse.json(document,{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Request failed"},{status:403});}}
