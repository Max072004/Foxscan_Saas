import { NextRequest,NextResponse } from "next/server"; import { getData,id,saveData,audit } from "@/lib/store"; import { currentUser,requireRole } from "@/lib/auth";
export async function GET(req:NextRequest){try{const data=await getData(),user=currentUser(data,req.headers.get("authorization"));return NextResponse.json(data.projects.filter(p=>p.tenantId===user.tenantId));}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unauthorized"},{status:401});}}
export async function POST(req:NextRequest){try{const data=await getData(),user=currentUser(data,req.headers.get("authorization"));requireRole(user,["ADMIN","CLIENT","CONSULTANT"]);const body=await req.json();const project={id:id(),tenantId:user.tenantId,status:"DRAFT" as const,...body};data.projects.push(project);await audit(data,{tenantId:user.tenantId,actorId:user.id,action:"CREATE",entity:"project",entityId:project.id,after:project});await saveData(data);return NextResponse.json(project,{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Request failed"},{status:403});}}
